import 'package:flutter/material.dart';

class BusDetailsRentTab extends StatelessWidget {
  const BusDetailsRentTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Rents")),
    );
  }
}
